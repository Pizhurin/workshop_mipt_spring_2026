# requirements.txt

# Ядро
pandas>=2.0.0
numpy>=1.24.0

# Preprocessing
# (использует встроенные json, re, logging — не требуют установки)

# Визуализация (для EDA)
matplotlib>=3.7.0
seaborn>=0.12.0

# Данные (чтение parquet)
pyarrow>=14.0.0
